/*
#include <qpShow.h>

void qpShow::draw() {

  if(this->ticks >= this->currentSceneLength)
    this->patternsController->playScene(this->moveToNextScene());

  this->patternsController->draw();
}
*/
