#include "newQpPattern.h"

void newQpPattern::render() {

    this->_frames++;

    this->draw();

    /*
    //update colors after rendering frame so not to skip initial color
    while(qpColor *currentColor = this->colors.fetch())
    (currentColor->*(currentColor->updateColorFunction))();
    */

}

void newQpPattern::activate() {
    this->onActivation();
    this->_activations++;
}

void newQpPattern::deactivate() {
    this->onDeactivation();
}